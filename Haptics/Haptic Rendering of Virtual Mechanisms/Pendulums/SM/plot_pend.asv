function plot_pend(ang)

% for i = 1:10
    r = 10;   
%     s = size(ang);
    x = r*cos(ang(1))
    y = r*sin(ang(1))
    
clf
    plot(x(1),y(1),'go'); hold on
    line([0;x], [0;y], 
    axis([-15 15 -15 15]);
%     pause(.75)
% end
end